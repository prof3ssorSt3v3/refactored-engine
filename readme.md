# Service Workers and PWA

clone the repo, but make sure that the `index.html`, `.gitignore`, and `sw.js` files are at the root of the folder that you have open in VS CODE.

When you launch your site with LiveServer, make sure to open the `Applications` Tab in **Chrome** and verify in the `Service Workers` tab that any old Service Worker has been replaced with our new one.
